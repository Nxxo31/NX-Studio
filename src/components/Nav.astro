---
const path = Astro.url.pathname;
const isActive = (p: string) => {
  if (p === '/') return path === '/NX-Studio/' || path === '/';
  return path.startsWith(p);
};
---

<header class="sticky top-0 z-50 border-b border-nx-border bg-nx-bg/80 backdrop-blur-xl">
  <nav class="mx-auto flex max-w-7xl items-center justify-between px-6 py-4">
    <a href="/NX-Studio/" class="flex items-center gap-2 font-display text-lg font-bold">
      <span class="grid h-8 w-8 place-items-center rounded-md font-mono text-xs text-black" style="background: linear-gradient(135deg, var(--lava-core), var(--lava-edge));">NX</span>
      <span>NX-Studio</span>
    </a>

    <ul class="hidden items-center gap-1 md:flex">
      <li>
        <a href="/NX-Studio/" class={`nav-link ${isActive('/NX-Studio/') && path.length < 15 ? 'nav-active' : ''}`}>Inicio</a>
      </li>
      <li><a href="/NX-Studio/cotizador" class={`nav-link ${isActive('/NX-Studio/cotizador') ? 'nav-active' : ''}`}>Cotizador</a></li>
      <li><a href="/NX-Studio/laboratorio" class={`nav-link ${isActive('/NX-Studio/laboratorio') ? 'nav-active' : ''}`}>Laboratorio</a></li>
      <li><a href="/NX-Studio/contacto" class={`nav-link ${isActive('/NX-Studio/contacto') ? 'nav-active' : ''}`}>Licitar</a></li>
    </ul>

    <a href="/NX-Studio/contacto" class="hidden md:inline-flex items-center gap-2 lava-btn" style="padding: 0.5rem 1.25rem;">
      Pedir propuesta →
    </a>

    <button id="mobile-toggle" class="md:hidden rounded-md border border-nx-border p-2" aria-label="Menu">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <line x1="3" y1="6" x2="21" y2="6"></line>
        <line x1="3" y1="12" x2="21" y2="12"></line>
        <line x1="3" y1="18" x2="21" y2="18"></line>
      </svg>
    </button>
  </nav>

  <div id="mobile-menu" class="hidden border-t border-nx-border md:hidden">
    <ul class="flex flex-col gap-1 px-6 py-4">
      <li><a href="/NX-Studio/" class="nav-link block">Inicio</a></li>
      <li><a href="/NX-Studio/cotizador" class="nav-link block">Cotizador</a></li>
      <li><a href="/NX-Studio/laboratorio" class="nav-link block">Laboratorio</a></li>
      <li><a href="/NX-Studio/contacto" class="nav-link block">Licitar</a></li>
    </ul>
  </div>
</header>

<script>
  const toggle = document.getElementById('mobile-toggle');
  const menu = document.getElementById('mobile-menu');
  toggle?.addEventListener('click', () => {
    menu?.classList.toggle('hidden');
  });
</script>